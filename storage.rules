
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Izin baca publik, izin tulis hanya untuk pengguna terotentikasi
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
